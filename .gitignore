# Build artifacts
bin/

# 前端依赖（web/ 与本地临时验证脚本安装的依赖都不入库）
node_modules/
web/dist.old/
coverage.out
coverage.html
coverage_git.out
debug.md

# pi config (contains auth tokens — never commit)
.pi/

.idea/
# 会话原始日志含敏感配置（config dump），不入库
raw_session_coding.log
