# Build artifacts
bin/
coverage.out
coverage.html
coverage_git.out
debug.md

# pi config (contains auth tokens — never commit)
.pi/

# Rick workspace temp (job-level smoke test artifacts)
.rick/jobs/job_3[2-9]/
