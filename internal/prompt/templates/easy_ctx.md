---

## 继承上下文（--ctx 模式）

本次会话继承了父级 .rick 上下文：`{{ctx_path}}`

### 第一步：启动 subagent 分析并迁移上下文

**在开始处理用户需求之前**，先启动一个 subagent 完成上下文初始化：

1. 读取父级 ctx 的以下文件（按优先级）：
   - `{{ctx_path}}/OKR.md`
   - `{{ctx_path}}/SPEC.md`
   - `{{ctx_path}}/wiki/*.md`（逐一读取）

2. 结合当前需求文档，在本地 .rick 目录（`{{local_rick_dir}}`）生成：
   - `{{local_rick_dir}}/OKR.md` — 以父级 OKR 为参考，结合当前需求起草本项目目标
   - `{{local_rick_dir}}/SPEC.md` — 以父级 SPEC 为架构模板，**剔除环境强相关内容**（见下方约束），补充当前项目技术细节

3. 对于父级 wiki 中的文档：有通用价值的迁移到本地 `{{local_rick_dir}}/wiki/`，项目特定的跳过

### ⚠️ 环境隔离约束（必须遵守）

父级 ctx 中以下内容**不得直接复制**，必须以当前环境为准重新配置：

| 内容类型 | 处理方式 |
|----------|----------|
| IP 地址、域名、端口 | 留空或标注 `TODO: 填写当前环境地址` |
| 密码、密钥、Token | 删除，标注 `TODO: 从当前环境获取` |
| 文件系统路径（/home/xxx、/opt/xxx） | 留空或标注 `TODO: 确认当前路径` |
| 数据库名、集群名 | 留空或标注 `TODO: 填写当前环境配置` |
| 服务账号、用户名 | 留空或标注 `TODO: 确认当前账号` |

**原则**：迁移的是架构知识和流程经验，不是环境配置。保持环境隔离。

### 知识查询规则

在会话过程中遇到模糊或不确定的信息时：
1. 先查当前 .rick（已迁移的上下文）
2. 若当前 .rick 无答案，**优先去父级 ctx 路径搜索**：`{{ctx_path}}`
3. 找到相关知识后，将其适配当前环境后使用

---
