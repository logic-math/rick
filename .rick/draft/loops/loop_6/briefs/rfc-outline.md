# RFC 大纲 — rick 三层金字塔架构重构 + spec 信息内核 + 功能下沉 pi — 2026-08-14

> 阶段一：大纲（先确认）。本文件只列章节骨架 + 每项一句话定位，不填具体实现细节。
> 阶段二（human 确认大纲后）再填内容。

---

## 主题

把 rick 重构为「三层金字塔架构（cli / handler / env+builder+runtime）」，将 doing 的 dag 调度与门禁检查下沉到 pi，并把「spec（结构化自然语言工程实现契约）」确立为 rick 的信息内核——使 subagent 触发确定性提升到上限内最高，且方法/实现隔离（丢弃源码即可 AI coding 出功能等价的 rick）。

---

## 背景与哲学基础

- **方法/实现隔离**：rick = 方法（自然语言描述），pi = 实现（编程语言描述）；方法描述经模型可转化为预期行为完全一致的开发计划。
- **核心假设**（human 显式自标）：只要自然语言无歧义地描述正确的验收标准，即可实现「方法与实现隔离、任意切换实现」；「等价」= 功能等价（通过所有功能验收即一致）。
- **协议对齐视角**：pi 与 rick 是两个各自自洽、但合作时暴露问题的「语言体系」；rick 现状 243 处自然语言触发词、0 处 pi 触发语法/内置 agent 名。
- **模型角色与进化迭代**（辩证逆转）：rick 不在乎模型能力上限；模型 = 信息压缩工具 / 信息模糊提取的存储器；所有解决老问题的信息被压缩进模型，rick 专注解决新问题；基于进化算法的持续迭代提升智能表现。

---

## 主要矛盾与辩证逆转

- N2 曾选「K4 深度定制 vs 框架独立性」为主要矛盾；S-R 辩证逆转后 human **否定 K4 是主要矛盾**——因 coding 让「实现」不再值钱、「方法」才值钱。
- 核心 = **工程化的方法描述**（方法与实现隔离）；实现「深度定制 与 独立迭代」的辩证逆转。
- rick 做薄 = 引导程序（sense 方法落地），dag 调度与门禁不再由 rick 维护，而是利用 pi 能力直接实现。

---

## 目标架构设计（三层金字塔）

### 分层

- **路由层 cli**：路由命令、解析参数等 cli 功能层。
- **处理层 handler**：接受 cli 路由与解析后的命令参数，调用 env / builder / runtime 三个模块完成 rick cmd 功能实现，是调度聚合层。
- **执行层 env + builder + runtime**：
  - **env**：对 rick 在当前机器的环境配置、关键依赖检查/下载/安装/配置；pi 及 pi 开源扩展与插件的维护与更新。
  - **builder**：按不同 cli 功能拼接、在 cmd 触发时创建一组符合 runtime（pi/dsh 等）要求规范的一组产物。
  - **runtime**：对 pi 或 dsh 等调用逻辑的封装（参数解析 + 调用，目前支持 pi runtime）。

### builder 三件

- **templates**：提示词模板，通过 go `embed` 内嵌二进制，按 cmd 功能拼接成某任务真正需要执行的提示词。
- **pibuilder**：为 pi 这一 runtime build 具体提示词文件目录的统一入口，内部组合 plan / doing / easy / human-loop 等多个子 builder。
- **xxxxbuilder**：可扩展位，后续新增 runtime 只需扩展这一 builder，其他组件不改动；与新 runtime 更好适配的信息封装在此层。

### runtime 设计

- 封装具体 agent runtime 的调用逻辑 = 参数解析 + 调用；目前支持 pi runtime。

### 设计原则（作为信息内核）

- 暂不考虑其他 builder/runtime；目标是让 rick 在执行层拥有更强控制力，深入 agent runtime 内部、更直接控制 LLM 输入信息，实现上下文熵减。
- 保持单一 runtime（pi）：投入全部精力将方法付诸实践，强化一种实现。
- 切换 runtime 的前提 = 新 runtime 带来更强大的生态与可定制性。

---

## 信息内核 = spec

- **spec** = 结构化自然语言描述的工程实现契约，是 rick 的信息内核。
- 后续 easy job 时，将信息内核以某种形式写入 `.rick/domain`；目标 = **丢弃一切源码，即可完全 AI coding 出一个功能等价的 rick**。

---

## 三个目标（O）与关键结果（KR）骨架

### O1：升级 rick 项目的 domain 描述方法，增加 spec 概念，并改造 rick 项目使其拥有这份 spec

- **KR1.1**：定义「spec」规范——结构化自然语言工程实现契约的模板与结构（模块边界 / 职责 / 接口契约 / 验收标准），作为 domain 描述方法升级的规范文档。
- **KR1.2**：产出 rick 的第一份 spec（覆盖三层金字塔架构 + 5 模块职责 + builder/runtime 契约 + 验收标准），并写入 `.rick/domain/`，使 rick 项目拥有这份 spec。
- **KR1.3**：定义 spec 的验收标准（「spec → 开发计划 → 功能等价实现」，功能等价 = 通过所有功能验收），作为 spec 质量的判定尺。

### O2：重构当前 rick 设计，使其符合三层金字塔架构

- **KR2.1**：落地三层金字塔包/目录结构（cli / handler / env + builder + runtime），handler 作为调度聚合层承接 cli 参数并编排三模块。
- **KR2.2**：抽出 env 模块（pi 及扩展依赖的检查/下载/安装/配置/维护更新，从现有 `tools_init_pi.go` 相关逻辑迁移）。
- **KR2.3**：重构 builder 三件——templates（go `embed` 内嵌现有 `internal/prompt/templates/` 模板）+ pibuilder（pi 统一入口，组合 plan/doing/easy/human-loop 等子 builder）+ xxxxbuilder（扩展位）。
- **KR2.4**：重构 runtime 模块，把 pi 调用逻辑（参数解析 + 调用，现有 `internal/agent/piagent/`）收口到 runtime 层。
- **KR2.5**：将 doing 的 dag 调度 agent 逻辑与门禁检查逻辑（现有 `internal/executor/` + `tools_doing_check.go` 等）下沉到 pi——利用 pi 能力直接实现 dag 调度与门禁，rick 中不再维护。

### O3：优化 pibuilder，将非三层金字塔架构部分的功能下沉到 pi

- **KR3.1**：pibuilder 产出符合 pi 定制化开发规范的提示词产物——pi agent 运行时所需的所有提示词**内聚在单文件内**，被 pi 以标准规范化定制开发语言引用使用。
- **KR3.2**：将 rick 方法中非三层金字塔架构部分的功能下沉到 pi（基于 pi 最佳定制化开发实践：扩展 / agent / 插件 / skill 等开放组件集成，如 think/research/exporter 注册为 pi 自定义 agent、门禁/dag 用 pi 编排能力实现）。
- **KR3.3**：把各命令模板中 243 处自然语言 subagent 触发词改写为显式 pi 触发语法（workflowScript + runs.run + 真实 agent 名），并显式化触发权归属与 SENSE 特有语义（编排权 parent、单写者、批判门禁、反向回流、判断记录）。

---

## 闭环逻辑说明

- **O1 ⇒** rick 拥有 spec（结构化工程实现契约），成为可被 AI 转化为等价开发计划的信息内核（丢弃源码即可重构）。
- **O2 ⇒** rick 变薄为引导程序（三层金字塔），dag 调度与门禁下沉 pi，执行层控制力增强。
- **O3 ⇒** pibuilder 深度对齐 pi 定制化开发规范 → 消除 E2/E3 边协议不对齐 → subagent 触发确定性提升。
- **O1+O2+O3 共同 ⇒** 触发确定性提升到上限内最高 + rick 做薄（sense 方法落地）+ 方法/实现隔离（spec 信息内核）。

**三缺口收敛（human 已辩证回答）**：
1. 量化评估——暂缺（当前靠直觉优化），假设暂时保留；后续专门思考如何评估 rick 优化效果。
2. 模型能力上限——已辩证逆转：基于进化算法持续迭代提升智能表现，不构成「上限内最高」的阻塞约束（rick 不在乎模型上限）。
3. 无歧义自然语言 ⇒ 等价开发计划——可接受假设；关键 = spec 对验收标准的无歧义表达（靠功能验收实测验证）。

---

## 派生修订需求

- S：期望由「最大化」修订为「提升到上限内最高」。
- N2 → S-R：K4「深度定制 vs 独立性」由主要矛盾修订为「非主要矛盾」。
- S-R：转义层架构收敛为「只保留单 runtime（pi）」，dsh 深定制时才架构升级。
- S-R：否定 research 缺口 1（tools 显式化）——tools 隐含在 skill 中，可忽略。
- EC 后：信息内核范围纠正为 rick 源码 md（`internal/prompt/templates/`），非 `.rick/` 运行时文件。
- 最终：信息内核上升为 spec（结构化自然语言工程实现契约，写入 `.rick/domain`）。

---

## 遗留逻辑漏洞（R7 上报项，待 human 决策 / 已收敛）

- 「触发概率低」的量化/复现证据（N3.3）与「提示词缺口 ⇒ 触发概率低」的因果归属（N3.4）——量化评估暂时保留（靠直觉优化），后续专门思考评估方法。
- 「spec 是否真正支撑丢弃源码、AI 重构出功能等价的 rick」（需实测）。
- 「无歧义自然语言 ⇒ 等价开发计划」——可接受假设，关键 = spec 对验收标准的无歧义表达（靠功能验收实测验证）。
- 模型能力上限——已辩证逆转（进化算法持续迭代），不再作为阻塞项。

---

## SENSE 流程记录

| 阶段 | 结果 |
|---|---|
| S 问题确认 | ✅ 通过（门禁第 2 轮） |
| E 视角生成 | ✅ 通过（视角 = 协议对齐 V5） |
| N1 矛盾生成 | ✅ 通过 |
| N2 主要矛盾判断 | ✅ 通过（选定 K4，后于 S-R 修订） |
| S-R 辩证逆转 | ✅ 通过（门禁第 2 轮） |
| EC 良知批判 | ✅ 跃迁 = 维持 |
