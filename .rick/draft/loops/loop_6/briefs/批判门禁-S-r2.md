# 批判门禁-S-r2 简报

**阶段**：S 问题确认 · 批判门禁 · 第 2 轮（think subagent 输出）
**主题**：subagent 在 pi runtime 下触发概率低，优化提示词。

## 输入（待审材料 · human 对第 1 轮 5 个未澄清点的回答，原话）

> 1. 我认为这个假设是成立的,可以继续推进
> 2. 是的，应改为提升到上限内最高
> 3. 其实没有排除，有可能是这个原因，需要实际改正完后再验证，或者你可以调研一下有什么事实可以佐证，可以搜索确认。
> 4. 我认为都是提示词未对齐 pi 的触发机制，这个主要原因。其他原因你可以调研一下，帮我确认。
> 5. 是的，关键应该是按 pi 的方式将其中几个 subagent 直接内置未系统级的 agent，定义好这些 agent，然后用明确的提示词进行触发。这样可以保证触发概率。这个可以调研一下

## 第 1 步：推理识别

| 推理段（原文） | 类型 | 说明 |
|---|---|---|
| "我认为这个假设是成立的"（因果归属） | 溯因 | 确认「缺口是主要原因」的归因判断 |
| "应改为提升到上限内最高" | 演绎 | 由「存在模型能力上限」推出目标表述应修正 |
| "其实没有排除，有可能是这个原因，需要实际改正完后再验证" | 溯因 | 模型能力差异作为残余原因，反推需实测验证 |
| "我认为都是提示词未对齐 pi 的触发机制，这个主要原因" | 归纳 | 多命令低触发 → 归因同一共性根因 |
| "内置为系统级 agent…用明确的提示词触发，这样可以保证触发概率" | 演绎 | 前提（系统级 agent + 显式触发）→ 结论（保证触发概率） |

## 第 2-3 步：假设提取 + 数量保障

共 7 个（≥ min_assumptions=5）。演绎 ×2（A2、A5）｜溯因 ×3（A1、A3、A6）｜归纳 ×1（A4）｜交叉 ×1（A7）——多视角覆盖（3 类推理各 ≥1 + 交叉 ≥1）。

## 第 4-5 步：形式化 + 4 维打分

| # | 推理类型 | 形式化 | 影响范围 | 不可逆性 | 影响程度 | 置信度 | 期望分 | 最终分 |
|---|---|---|---|---|---|---|---|---|
| 1 | 归纳 | 如果 [各命令低触发是同一根因（提示词未对齐）] 成立，那么 [统一修提示词对齐即可覆盖各命令] 成立 | 1.0 | 0.5 | 1.0 | 0.7 | 0.55 | 0.55 |
| 2 | 演绎 | 如果 [将 think/research/exporter 注册为系统级 agent 并显式 agent 名触发] 成立，那么 [触发概率得到保证/确定性显著提升] 成立 | 1.0 | 0.5 | 1.0 | 0.7 | 0.55 | 0.55 |
| 3 | 溯因 | 如果 [注册系统级 agent + 显式语法触发方案对 rick 可实施] 成立，那么 [该方案是 rick 的可行改进路径] 成立 | 0.83 | 0.5 | 1.0 | 0.7 | 0.55 | 0.46 |
| 4 | 溯因 | 如果 [结构性缺口是触发概率低的主要原因] 成立，那么 [对齐提示词+注册系统级 agent 能显著提升触发确定性] 成立 | 1.0 | 0.5 | 1.0 | 0.6 | 0.40 | 0.40 |
| 5 | 演绎 | 如果 [触发确定性存在由模型能力决定的固有上限] 成立，那么 [目标应设为"提升到上限内最高"而非"最大化"] 成立 | 0.83 | 0.5 | 0.5 | 0.9 | 0.40 | 0.33 |
| 6 | 交叉 | 如果 [提示词对齐 + 系统级 agent 双管齐下] 成立，那么 [触发确定性可提升到上限内最高] 成立 | 1.0 | 0.5 | 1.0 | 0.5 | 0.25 | 0.25 |
| 7 | 溯因 | 如果 [模型 tool-calling 能力差异是触发概率低的残余原因] 成立，那么 [改正提示词后仍需实测验证模型能力上限] 成立 | 0.5 | 0.5 | 0.5 | 0.4 | -0.10 | -0.05 |

> 置信度更新说明（只被 research 结果更新，先验：演绎 0.9 / 归纳 0.6 / 溯因 0.4 / 交叉 0.5）：
> - A4（归纳 0.6→0.7）：research 证实 150 处自然语言术语为各命令共性 + 配置层已排除 + human 显式确认，但「各命令失败点完全一致」仍属归纳，仅小幅上调。
> - A5（演绎 0.9→0.7）：机制前提已被 research 证实（B3 显式 agent 名触发 + C1/C2 显式优于软指令），但 Y「保证触发概率」是强断言，research 只证「更可靠」未证「保证」，故下调。
> - A6（溯因 0.4→0.7）：B1/B2/B3 证实自定义 agent 机制完全可实施、B4 证实 rick 现状可注册，X 已被高置信证实。
> - A1（溯因 0.4→0.6）：X「缺口存在」已证实（N3.1/N3.2 + C1/C2），human 显式确认因果，但因果归属本质仍属假设（N3.4 R7），仅上调至 0.6。
> - A3（溯因 0.4 不变）：A1 模型能力差异本地无 benchmark 信源（R7），research 无法更新。

## 第 6 步：top-N（N=3）

top-N：**#1（A4，0.55）、#2（A5，0.55）、#3（A6，0.46）**。
第 4 名（A1=0.40）与第 3 名（A6=0.46）差距 13% > 10%，不纳入；A7（0.25）、A3（-0.05）最终分 < 0.3 不入选（A3 仍列为待验证项，见门禁结论）。

## top-N 假设的 3 启发性问题

### 假设 #1：如果 [各命令低触发是同一根因（提示词未对齐）] 成立，那么 [统一修提示词对齐即可覆盖各命令] 成立
- Q1 信念：关于"各命令低触发是同一根因、可统一修复"，你内心最确信的是什么？最不确定的是什么？
- Q2 前提："统一修提示词即可覆盖各命令"成立需要什么前提（例如：各命令的 subagent 触发失败点确实相同、没有命令特有的独立阻塞）？这些前提你确认过吗？
- Q3 反例：什么证据会让你改变"同一根因"的判断？（例如：某命令改正后触发率仍低，暴露出另一处独立配置阻塞）

### 假设 #2：如果 [将 think/research/exporter 注册为系统级 agent 并显式 agent 名触发] 成立，那么 [触发概率得到保证/确定性显著提升] 成立
- Q1 信念：关于"注册系统级 agent + 显式 agent 名触发能保证触发概率"，你最确信与最不确定的边界分别在哪里？
- Q2 前提："保证触发概率"成立需要什么前提（例如：模型看到显式 agent 名后确实会调用 subagent 工具，而非继续忽略软指令）？这些前提你确认过吗？
- Q3 反例：什么证据会让你改变"系统级 agent 能保证触发"的判断？（例如：实测触发率仍未达上限内最高）

### 假设 #3：如果 [注册系统级 agent + 显式语法触发方案对 rick 可实施] 成立，那么 [该方案是 rick 的可行改进路径] 成立
- Q1 信念：关于"系统级 agent 方案对 rick 可实施"，你最确信与最不确定的分别是什么？
- Q2 前提："方案可实施"需要什么前提（例如：rick 能把 agent 定义落盘到 pi 发现目录、提示词能引用真实 agent 名而不与隔离环境冲突）？这些前提你确认过吗？
- Q3 反例：什么证据会让你改变"方案可实施"的判断？（例如：pi agent 发现目录与 rick 隔离环境冲突、或 agent 名与内置名冲突）

**→ human 请思考并回答（可选择性回答，不必全答）。是否需要派 research 调研 X？**

## 门禁结论：✅ 通过

top-N（A4/A5/A6）的 Y 均已澄清或显式确认：
- **A4（同一根因）**：human 第 4 点显式确认「都是提示词未对齐 pi 触发机制」；research 佐证（150 处共性 + 配置层已排除）。
- **A5（系统级 agent 触发）**：human 第 5 点显式给出方向；research B1/B2/B3 证实机制，B4 证实 rick 可注册。
- **A6（方案可实施）**：research B1-B4 高置信澄清，X 已被证实。

**随 S 阶段带入下阶段的待验证项（非阻塞，human 已明确承接）**：
1. A3（模型能力差异是否构成残余上限）→ human 第 3 点明确「改正后验证」，research 标注 R7（本地无 benchmark 信源）。
2. N3.3（触发概率低的量化/复现）→ human 观察，改正后实测。
3. N3.4（因果归属实测）→ human 第 1 点确认因果成立，但本质仍属假设，改正后实测收敛。

**残余观察（不阻塞）**：A5 的 Y「保证触发概率」为强断言，research 仅支持「更可靠」而非「保证」；human 已在第 2/3 点以「上限内最高 + 改正后验证」软化此断言，语义一致。该项随 S 阶段作为"待实测验证"带入，不构成未澄清假设。
