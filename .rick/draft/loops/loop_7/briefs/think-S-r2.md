# think-S-r2 简报

> 阶段:S 问题确认 第 2 轮(loop_7)| 主题:事实性模糊消解(pi vs dsh runtime)| 输入:research-S-r2.md+think-S.md

## 一、已消解:Q5 模板耦合(原唯一事实性架构假设)

**结论(不再作为问题)**:「runtime=薄接口、切换成本 confined 于 Go seam」不成立——templates 是第二耦合面,「templates 不改」承诺须修订。证据:
- 写侧:显式 pi 触发语法 128 处/19 文件,11 文件含可执行 JS(sense_loop.md 18 处最重);`agent:'xxx'` 约 75 处、timeoutMs 27 处;agent 名含 pi-subagents 内置 worker/reviewer/researcher(第三方依赖)
- 读侧:`.pi/subagents/artifacts/` 布局+meta.json/transcript.jsonl 约 11 处/4 文件(ctrl/dream/doing/learning)
- 路径:ctrl.md 2 处硬编码 `~/.rick/pi/agent/sessions/...`
- 缓解:compaction 假设 0 处——不涉上下文管理行为
修订方向:templates 分 runtime 版本,或 dsh 侧等价 subagent API+兼容产物布局。

## 二、保留的判断性问题(top-4)

### Q1 重开已决事项的触发条件
- 问题:若「loop_7 现在就要判断谁适合做 runtime」成立,那么也假设了「loop_6 之后出现了足以重开已决事项的新条件」——这真的正确吗?新数据不构成结构性新材料:dsh 发布(08-13)早于 loop_6 判断(08-14),其后仅 rc 迭代;TB2.1 官方 82.70% 与社区复测 0.51 冲突未归因。
- 依据:research-S-r2 ③-1/③-2;research-S ②-8;loop_6 已决单一 runtime=pi。
- 改变判断的证据:dsh 发 1.0+插件市场成形,pi 停更/bus factor 兑现,或 TB2.1 归因后 dsh 显著领先。
- 性质:判断性(触发条件=human 意图);事实部分两轮均无新触发。

### Q2 「短期性能」的指代与杠杆(依据更新)
- 问题:若「对比 pi/dsh 能回答短期如何提升性能」成立,那么也假设了「短期性能有明确指代维度,且 runtime 选择是该维度的有效杠杆」——这真的正确吗?R7-4 已证明度量可纯离线落地(现存 5 数据源),「基线缺失」从不可解变为可解但尚未运行;直接受控对比仍不存在。
- 依据:research-S-r2 ②(触发率/启动率/延迟 P50/P95/成功率/成本均可答)+③-5(直接对比仍无;dsh 延迟 issue #3235/#3304 仅单侧)。
- 改变判断的证据:R7-4 脚本运行后基线显示瓶颈在 runtime 层(如触发不确定性根因在 pi-subagents),或直接对比出现且 dsh 显著优。
- 性质:判断性(维度取舍须 human 定:触发确定性/上下文效率/延迟/成本);事实性子项「无直接对比」**未消解**。

### Q3 长期架构绑定 vs spec 可切换赌注
- 问题:若「长期架构发展取决于选谁做 runtime」成立,那么也假设了「spec 可切换性不足以消解 runtime 锁定风险」——这真的正确吗?两侧证据形态不对称:切换成本已被 Q5 量化(templates 128 处+产物+路径),spec 可消解侧仍无实验验证。若 spec 赌注成立,runtime 选择长期廉价可逆;若不成立,切换成本逐轮复利。两方向不能同时为真。
- 依据:research-S-r2 ①;research-S ①-6/①-7(spec 已落地、dsh seam 预留)。
- 改变判断的证据:小规模 spec→等价重实现实验(用 spec 重建 rick 某模块并过验收)。
- 性质:判断性;可设计实验但不属调研。

### Q4 评判标准集的完备性
- 问题:若「按生态+可定制性两标准对比即可得出结论」成立,那么也假设了「该标准集完备、无遗漏致命维度」——这真的正确吗?成熟度/稳定性(dsh developer preview+breaking changes)与维护者风险(pi bus factor=1)不在集合内;新增:TB2.1 数据可信度争议、pi-subagents 依赖证实已进 templates。
- 依据:research-S-r2 ①/③-1;research-S ②-8/③-9/①-4。
- 改变判断的证据:dsh 发 1.0 且 pi 形成第二梯队维护者;TB2.1 归因。
- 性质:判断性(标准加权是价值选择);事实已供齐。

## 三、S 三连总追问

- 现状:你心中的「现状」是「loop_6 修复已落地且效果可感知」,还是「落地但效果未知」?若是后者——度量现已可离线算出,「性能仍需提升」的判断准备落在运行基线上还是直觉上?
- 期望:本轮期望产出是(A)重选 runtime 的决定,还是(B)校验维持 pi+定义「未来切换触发器」?两者所需证据强度完全不同;新事实仅量化 A 的成本下限(templates 须分版本),不改变选择结构。
- 差距:真正的差距是「对 pi/dsh 的认知差距」(直接对比仍缺),还是「对 rick 自身效果的度量差距」(方案已有、尚未运行)?若是后者,深度学习 dsh 无法消解它,度量脚本运行才是本轮最短缺的交付。

## 四、未消解项清单(仅剩调研无法消解的)

1. dsh vs pi 直接受控对比不存在——Q2 反例未触发;子项「pi 是否有 TB2.1 条目」可再查,对比本身须实验。(事实性,未消解)
2. TB2.1 官方 82.70% vs 社区 0.51 未归因——以 TB2.1 为 dsh 质量证据暂不可用,须官方 artifacts 或复现归因。(事实性,未消解)
3. R7-4「预期派发数」无机器可读定义——度量落地唯一前置;工程任务非调研。
4. workflowScript→dsh ctx.subagents 等价性未实测(R7-5 spike)——Q5 结论的 dsh 侧验证;实验非调研。

已消解移出:Q5(→一)、R7-4 可行性(→Q2)、R7-6 存在性(→Q1/Q4)。
