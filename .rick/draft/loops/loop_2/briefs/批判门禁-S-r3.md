## 批判门禁 简报（S 阶段第 3 轮，重试 3/5）

**输入**（human S 阶段三次调研后三连追问实质回答原话）：

> 1. a,b,c 这三个问题都存在; 很多需要 hook 的能力是可以内置到循环内部去做的; 比如更好的 tdd 循环，以及 debug，这些都可以通过 ts 扩展，增强执行的确定性; 比如所有的变更都会执行 tdd 验证，然后确定性地更新 DAG 状态，等等。在一个 session 会话内部，启动 subagent 的过程也可以使其更具有确定性，更好的控制上下文避免污染。
> 2. a，b，c，d，e，f 都是我需要的。凡是需要内嵌到 agent loop 中的功能 rick 都无法实现。内嵌 rick 的好处在于，可以自定义给 LLM 的所有 input，pi 对 LLM 的入侵极小，上下文的内容可以更深度的自定义。实现对上下文的深度编排，将 prompt 的 loop 流程，等功能作为系统提示词，compaction 压缩时需要保留不被压缩到，确保最终目标的一致性，debug 的时候可以精细的控制 debug 方法作为系统提示词。比如对于流程与方法作为系统提示词，后续行为执行的过程中，也要及时的对关键信息进行压缩。让他能够确定性的，更长程的进行 debug，一步步调查事实最终达成目标。
> 3. 我接受重构，要做到字段级功能的等价。

**前序上下文**（research round 1+2+3 高置信事实，置信度 0.9）：
- ai_cli = `internal/agent/` 抽象层；claude code 13 处调用点（仅 1 处走接口）
- pi 是 Node.js ≥22.19.0（非 Go），无 Go binding；支持 Bun 编译 standalone binary（30-44 MB，6 平台预编译）
- pi 6 类扩展点（Prompt Templates/Skills/Extensions/Themes/Pi Packages/Agent Core 钩子）
- pi 流式字段 schema 与 rick NDJSON 5 项不对齐 + duration_ms 缺失
- pi 显式 No MCP/No sub-agents/No permission/No plan-mode（设计哲学宣言）
- pi RPC 长连接模式 + steering/followUp 消息队列
- Y1 ✅: standalone binary 自包含成立（30-44 MB 6 平台预编译，真零 Node 依赖）
- Y3 ✅(部分): registerTool 是真系统级注册，但 rick skill 流程描述 ≠ pi tool 函数签名，需重写为 TS extension
- Y5 ✅: pi 内置 30+ provider，per-prompt 模型切换原生支持，per-task 路由 = CLI flag/env var，无需多 pi 进程
- Y6 ✅: pi 官方提供 subagent extension 范例，每 subagent 独立 pi 进程，支持 single/parallel/chain 三模式

**第 2 轮门禁已识别的 Y**：Y1/Y3/Y5/Y6 事实性（已澄清）+ Y2 价值性（本次给出证据：a/b/c 三瓶颈并存 + a/b/c/d/e/f 全要）

---

### 推理类型识别

| 段 | 推理类型 | 形式 |
|---|---|---|
| "a,b,c 三个问题都存在 → hook 能力内置循环 → 增强执行确定性" | 演绎 | 前提 A（hook 内置）+ 前提 B（TDD/DAG/subagent 确定性）→ 结论（执行确定性增强） |
| "tdd 验证 + DAG 状态更新 → 确定性" | 演绎 | 因果链：TDD/DAG → 确定性 |
| "subagent 启动过程更确定性 → 控制上下文避免污染" | 演绎 | 因果链：subagent 确定性 → 上下文无污染 |
| "凡需内嵌 agent loop 的功能 rick 都无法实现" | 归纳 | 从"skill/compaction/subagent 等"n 案例推广到"凡需内嵌的都无法" |
| "pi 对 LLM 入侵极小 → 可自定义给 LLM 的所有 input" | 溯因 | 现象（pi 入侵小）→ 反推解释（可深度自定义 input） |
| "流程/方法作为系统提示词 + compaction 保留不被压缩 → 确定性长程 debug" | 演绎 | 因果链：system prompt + compaction 保留 → 长程 debug 确定性 |
| "接受重构 → 字段级功能等价" | 演绎 | 条件结论：字段级等价 → 接受重构 |
| "a/b/c/d/e/f 全要 → 同时做" | 归纳 | 从"全要"推广到"同时实现"（vs 分阶段） |
| "rick 现有 doing_loop.md 内嵌 doing.md → 不算内嵌 agent loop" | 溯因 | 现象（doing_loop 内嵌）→ 反推解释（语义边界：模板内嵌 ≠ loop 内嵌） |

---

### 假设列表（共 10 个，满足 min_assumptions=5 + 多视角强制：演绎×4 + 归纳×2 + 溯因×2 + 交叉×2）

| # | 推理类型 | 形式化 | 影响范围 | 不可逆性 | 影响程度 | 置信度 | 期望分 | 最终分 |
|---|---|---|---|---|---|---|---|---|
| 1 | 演绎 | 如果 hook 能力内置循环（TDD 验证/DAG 状态更新/subagent 确定性），那么执行确定性增强 | 1.00 | 0.5 | 1.0 | 0.6 | 0.40 | 0.40 |
| 2 | 演绎 | 如果流程/方法作为系统提示词 + compaction 保留不被压缩，那么确定性长程 debug 成立 | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |
| 3 | 归纳 | 如果凡需内嵌 agent loop 的功能 rick 都无法实现（skill/compaction/subagent n 案例），那么替换 pi 是唯一路径 | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |
| 4 | 溯因 | 如果 pi 对 LLM 入侵极小，那么可自定义给 LLM 的所有 input（vs claude code 有固定行为） | 0.83 | 0.5 | 1.0 | 0.4 | 0.10 | 0.08 |
| 5 | 演绎 | 如果字段级功能等价（验收标准），那么语义级差异（subagent 隔离强度/skill 触发机制）被忽略 | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |
| 6 | 归纳 | 如果 a/b/c/d/e/f 全要，那么六者同时做（vs 分阶段优先级） | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |
| 7 | 溯因 | 如果 rick 现有 doing_loop.md 内嵌 doing.md，那么"内嵌 agent loop"语义边界是"loop 逻辑层内嵌"而非"模板文本内嵌" | 0.83 | 0.5 | 1.0 | 0.4 | 0.10 | 0.08 |
| 8 | 交叉（演绎+溯因） | 如果 pi compaction 机制不保留 system prompt，那么"流程作为系统提示词 + compaction 保留"因果链断裂 | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |
| 9 | 演绎 | 如果 subagent 启动过程更确定性（pi 独立进程 + 单/并行/链式三模式），那么上下文污染可避免 | 0.83 | 0.5 | 1.0 | 0.9 | 0.95 | 0.79 |
| 10 | 交叉（归纳+演绎） | 如果 hook 内置循环依赖 TS 扩展，那么 rick 现有 Go 模板体系（doing.md/doing_loop.md）需全面重写为 TS | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |

**打分明细**（影响范围 = (决定性 + 根本性 + 全局性)/3，每子维 1.0/0.5）：

- **#1**: 决定性 1.0（确定性是执行质量根本）+ 根本性 1.0 + 全局性 1.0（影响 doing/learning/dream 全阶段）= 1.00 | 不可逆 0.5（可调整 hook 设计）| 影响 1.0（高赔率，直接解决 a/b/c）| 置信 0.6（归纳因果链，hook → 确定性未实测）→ 期望 = (1.0×0.6) - (0.5×0.4) = 0.40；最终 = 0.40×1.00 = 0.40
- **#2**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（system prompt 设计一旦定下重构面广）| 影响 1.0 | 置信 0.6（归纳，compaction 保留 system prompt 未确认）→ 期望 = 0.6 - 0.4 = 0.20；最终 = 0.20×1.00 = 0.20
- **#3**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（若"唯一路径"判断错误，替换决策前提崩塌）| 影响 1.0 | 置信 0.4（溯因，n=3 案例不足推广"凡需内嵌"全称命题）→ 期望 = (1.0×0.4) - (1.0×0.6) = -0.20；最终 = -0.20×1.00 = -0.20
- **#4**: 决定性 1.0 + 根本性 1.0 + 全局性 0.5（仅 input 控制维度）= 0.83 | 不可逆 0.5 | 影响 1.0 | 置信 0.4（溯因，"pi 入侵极小"是宣称 vs claude code 固定行为未对比验证）→ 期望 = (1.0×0.4) - (0.5×0.6) = 0.40 - 0.30 = 0.10；最终 = 0.10×0.83 = 0.08
- **#5**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（字段级等价忽略语义级，迁移后行为偏差难追溯）| 影响 1.0 | 置信 0.6（归纳，字段 vs 语义边界未明）→ 期望 = 0.6 - 0.4 = 0.20；最终 = 0.20×1.00 = 0.20
- **#6**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（同时做六者，资源分散风险）| 影响 1.0 | 置信 0.4（溯因，human 说"全要"但未说"同时"，分阶段可能性存在）→ 期望 = (1.0×0.4) - (1.0×0.6) = -0.20；最终 = -0.20×1.00 = -0.20
- **#7**: 决定性 1.0 + 根本性 0.5（语义边界澄清）+ 全局性 1.0 = 0.83 | 不可逆 0.5 | 影响 1.0 | 置信 0.4（溯因，"内嵌"语义边界是 human 隐含定义，未显式确认）→ 期望 = (1.0×0.4) - (0.5×0.6) = 0.10；最终 = 0.10×0.83 = 0.08
- **#8**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（若 compaction 不保留 system prompt，核心论点断裂）| 影响 1.0 | 置信 0.4（溯因，pi compaction 机制是否保留 system prompt 未 research）→ 期望 = (1.0×0.4) - (1.0×0.6) = -0.20；最终 = -0.20×1.00 = -0.20
- **#9**: 决定性 1.0 + 根本性 1.0 + 全局性 0.5（仅 subagent 维度）= 0.83 | 不可逆 0.5（subagent 模式可调整）| 影响 1.0（高赔率，解决上下文污染）| 置信 0.9（演绎，research 已确认 pi 官方 subagent extension + 独立进程 + 三模式）→ 期望 = (1.0×0.9) - (0.5×0.1) = 0.95；最终 = 0.95×0.83 = 0.79
- **#10**: 决定性 1.0 + 根本性 1.0 + 全局性 1.0 = 1.00 | 不可逆 1.0（Go 模板 → TS 重写，工作量大且双向迁移成本高）| 影响 1.0 | 置信 0.6（归纳+演绎，hook 内置依赖 TS 已确认 + rick 现有 Go 模板体系已确认）→ 期望 = 0.6 - 0.4 = 0.20；最终 = 0.20×1.00 = 0.20

**排序**（最终分降序）：

| # | 推理类型 | 形式化 | 影响范围 | 不可逆性 | 影响程度 | 置信度 | 期望分 | 最终分 |
|---|---|---|---|---|---|---|---|---|
| 9 | 演绎 | 如果 pi subagent 独立进程 + 三模式，那么上下文污染可避免 | 0.83 | 0.5 | 1.0 | 0.9 | 0.95 | 0.79 |
| 1 | 演绎 | 如果 hook 内置循环（TDD/DAG/subagent），那么执行确定性增强 | 1.00 | 0.5 | 1.0 | 0.6 | 0.40 | 0.40 |
| 2 | 演绎 | 如果 system prompt + compaction 保留，那么确定性长程 debug | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |
| 5 | 演绎 | 如果字段级等价，那么语义级差异被忽略 | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |
| 10 | 交叉 | 如果 hook 内置依赖 TS，那么 Go 模板需全面重写为 TS | 1.00 | 1.0 | 1.0 | 0.6 | 0.20 | 0.20 |
| 4 | 溯因 | 如果 pi 入侵极小，那么可自定义所有 input | 0.83 | 0.5 | 1.0 | 0.4 | 0.10 | 0.08 |
| 7 | 溯因 | 如果 doing_loop 内嵌 doing.md，那么"内嵌"边界是 loop 逻辑层 | 0.83 | 0.5 | 1.0 | 0.4 | 0.10 | 0.08 |
| 3 | 归纳 | 如果 n=3 案例，那么"凡需内嵌 rick 都无法"全称命题成立 | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |
| 6 | 归纳 | 如果 a/b/c/d/e/f 全要，那么同时做 | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |
| 8 | 交叉 | 如果 pi compaction 不保留 system prompt，那么核心论点断裂 | 1.00 | 1.0 | 1.0 | 0.4 | -0.20 | -0.20 |

**top-N 选择**（N=3，浮动+阈值）：
- 第 1 名 #9（0.79），第 2 名 #1（0.40），第 3 名 #2/#5/#10 同分 0.20 — 第 3 名与第 4/5/6 名差距 0% < 10% → 浮动纳入 → top-N 候选 = #9, #1, #2, #5, #10
- #4/#7（0.08）< 0.3 阈值 → 不入选
- #3/#6/#8（-0.20）< 0.3 → 不入选（全负值风险提示）
- 阈值过滤后：#9（0.79）, #1（0.40）入选（≥0.3）；#2/#5/#10（0.20）< 0.3 阈值 → 严格按阈值不入选，但作为风险提示

**最终 top-N = 2**（#9, #1）；#2/#5/#10 虽 < 0.3 但不可逆性 1.0，列为风险提示

---

### top-N 假设的 3 启发性问题

#### 假设 #9：如果 pi subagent 独立进程 + 单/并行/链式三模式，那么上下文污染可避免
- **Q1 信念**：关于"上下文污染可避免"，你内心最确信的是什么？最不确定的是"独立进程"是否真能隔离 LLM context（而非仅进程隔离）——pi subagent 的 prompt 隔离 / tool 权限 / 结果回传 / trace 继承机制是否与 rick 现有 subagent 语义等价？
- **Q2 前提**：成立需要什么前提？你是否确认 pi 官方 subagent extension 范例的 prompt 隔离强度（subagent 是否能看到父 agent 的 message history）？rick 现有 subagent（plan/doing/learning/dream 各阶段派发）的语义契约是什么——结果回传后父 agent context 是否真不污染？这些前提你确认过吗？
- **Q3 反例**：什么证据会让你改变判断？——若 research 发现 pi subagent extension 的 prompt 隔离是"prompt 字符串拼接"而非"独立 context window"，或 subagent 结果回传会注入父 agent message history（仍可能污染），你还会认为"上下文污染可避免"成立吗？

#### 假设 #1：如果 hook 能力内置循环（TDD 验证/DAG 状态更新/subagent 确定性），那么执行确定性增强
- **Q1 信念**：关于"执行确定性增强"，你内心最确信的是什么？最不确定的是"确定性"的度量基准——是 task 完成率提升 / 行为轨迹可复现 / 还是 DAG 状态更新的原子性？
- **Q2 前提**：成立需要什么前提？你是否假设了"当前 a/b/c 瓶颈的根因是 hook 不足"而非"prompt 设计/任务分解/模型能力"？pi 的 hook 机制（transformContext/beforeToolCall/registerTool）是否能注入 TDD 验证逻辑（执行外部 test 命令 + 解析结果 + 更新 DAG）？TDD 验证失败时 hook 能否阻止 LLM 继续下一步？这个因果链你确认过吗？
- **Q3 反例**：什么证据会让你改变判断？——若 research 发现 pi hook 机制是"事件通知"而非"执行拦截"（hook 不能阻止 LLM 行为，只能记录），或 TDD 验证需调用外部 test runner 但 hook 内无法执行同步阻塞 IO，你还会认为"hook 内置 → 确定性增强"成立吗？

---

### 风险提示（非 top-N 但需关注，均不可逆性 1.0）

- **#2（最终分 0.20，不可逆 1.0）**："流程/方法作为系统提示词 + compaction 保留不被压缩"是 human 核心论点。但 pi compaction 机制是否保留 system prompt 未 research。若 compaction 压缩时也裁剪 system prompt（仅保留最近 N 条 message），核心论点断裂。**需 research 四次调研 pi compaction 机制是否保留 system prompt**。
- **#5（最终分 0.20，不可逆 1.0）**：human 验收标准是"字段级功能等价"，但语义级差异（subagent 隔离强度是 prompt 拼接 vs 独立 context / skill 触发是 CLI 命令 vs LLM tool schema / hook 是事件通知 vs 执行拦截）可能被字段对齐掩盖。迁移后行为偏差难追溯。**需 human 明确"字段级等价"是否覆盖语义级**。
- **#10（最终分 0.20，不可逆 1.0）**：hook 内置循环依赖 TS 扩展，rick 现有 Go 模板体系（doing.md/doing_loop.md/learning.md/dream.md + embed.FS + WriteSkillFileWithVars）需全面重写为 TS。工作量大且双向迁移成本高。**需 human 确认是否接受 Go 模板体系废弃**。
- **#3（最终分 -0.20，不可逆 1.0）**："凡需内嵌 agent loop 的功能 rick 都无法实现"是全称命题，但 n=3 案例不足以推广。rick 现有 doing_loop.md 内嵌 doing.md 是否算"内嵌 agent loop"的语义边界未澄清（#7）。若 doing_loop 内嵌算"内嵌"，则全称命题有反例。**需 human 明确"内嵌 agent loop"的语义边界**。
- **#6（最终分 -0.20，不可逆 1.0）**：a/b/c/d/e/f 全要，但未说"同时做"还是"分阶段"。同时做六者资源分散风险。**需 human 明确优先级与阶段性**。
- **#8（最终分 -0.20，不可逆 1.0）**：与 #2 同源，pi compaction 不保留 system prompt 则核心论点断裂。**需 research 四次调研**。

---

### 门禁结论

❌ **未通过**。以下 Y 未澄清：

1. **Y2（深度控制 → 更好效果，价值性假设）**：✅ **本次已澄清**——human 明确 a/b/c 三瓶颈并存（skill 触发率低 + 长程 context 丢失 + task 完成率低）+ a/b/c/d/e/f 全要。**价值性假设根因已确认**：瓶颈是"控制不足"（hook 内置 + system prompt + compaction 保留 + subagent 确定性可解决），而非"prompt 设计/任务分解"。**第 1/2 轮未澄清的 Y2 本轮闭环**。

2. **Y7（pi compaction 是否保留 system prompt）**：**新发现未澄清**——human 核心论点"流程/方法作为系统提示词 + compaction 保留不被压缩 = 确定性长程 debug"依赖 pi compaction 机制保留 system prompt。但 research 1/2/3 未调研此机制。若 compaction 压缩时也裁剪 system prompt，核心论点断裂。**需 research 四次调研 pi compaction 机制**。

3. **Y8（"内嵌 agent loop" 语义边界）**：**新发现未澄清**——human 论点"凡需内嵌 agent loop 的功能 rick 都无法实现"是全称命题。但 rick 现有 doing_loop.md 内嵌 doing.md（模板文本内嵌）是否算"内嵌 agent loop"？若算，全称命题有反例；若不算（语义边界是"loop 逻辑层内嵌"），需 human 明确。**需 human 显式定义"内嵌 agent loop"的语义边界**。

4. **Y9（字段级等价 vs 语义级等价）**：**新发现未澄清**——human 验收标准是"字段级功能等价"，但语义级差异（subagent 隔离强度 / skill 触发机制 / hook 拦截能力）可能被字段对齐掩盖。**需 human 明确"字段级等价"是否覆盖语义级**。

5. **Y10（a/b/c/d/e/f 优先级与阶段性）**：**新发现未澄清**——human 说"全要"但未说"同时做"还是"分阶段"。同时做六者资源分散风险。**需 human 明确优先级与阶段性**。

6. **Y11（Go 模板体系废弃接受度）**：**新发现未澄清**——hook 内置循环依赖 TS 扩展，rick 现有 Go 模板体系（doing.md/doing_loop.md/learning.md/dream.md + embed.FS + WriteSkillFileWithVars）需全面重写为 TS。human 说"接受重构"但是否接受 Go 模板体系废弃（双向迁移成本高）未确认。**需 human 明确 Go 模板体系废弃接受度**。

**建议下一步**：**混合模式**——
- **research 四次调研**（澄清事实）：pi compaction 机制是否保留 system prompt（Y7）——这是 human 核心论点的关键依赖，未 research 则门禁无法通过
- **human 直接回答**（澄清语义/边界/优先级）：
  - (a) "内嵌 agent loop" 的语义边界（Y8）——doing_loop.md 内嵌 doing.md 算不算？
  - (b) "字段级等价" 是否覆盖语义级（Y9）——subagent 隔离强度 / skill 触发机制 / hook 拦截能力的语义差异是否要求等价？
  - (c) a/b/c/d/e/f 的优先级与阶段性（Y10）——同时做还是分阶段？哪个先？
  - (d) Go 模板体系废弃接受度（Y11）——doing.md/doing_loop.md/learning.md/dream.md + embed.FS + WriteSkillFileWithVars 全面重写为 TS，是否接受？

**→ human 请思考并回答上述 2×3=6 个启发性问题（可选择性回答，不必全答）。是否派 research 四次调研 pi compaction 机制（Y7）？是否直接回答 Y8/Y9/Y10/Y11 的语义边界与优先级？**
