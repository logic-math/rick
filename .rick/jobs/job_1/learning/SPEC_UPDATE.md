# SPEC 更新建议

## 更新理由

基于 Job 1 的执行经验，我们发现了一些需要在 SPEC 中明确的规范和最佳实践。这些规范将帮助未来的任务执行更加高效、准确，避免重复出现的问题（如路径歧义、任务粒度不当等）。

## 具体更新内容

### 新增规范

#### 技术栈

- 语言: Go 1.21+
- 框架: 标准库为主，最小化外部依赖
- 文档工具: Markdown + Mermaid
- 测试框架: Go testing 标准库 + Python 测试脚本
- 版本控制: Git

#### 架构设计

- 架构风格: 模块化单体架构，按功能划分模块
- 模块划分: cmd（命令处理）、workspace（工作空间）、parser（解析）、executor（执行引擎）、prompt（提示词管理）、git（Git 操作）、config（配置管理）、callcli（Claude Code CLI 交互）
- 接口设计: 模块间通过清晰的接口交互，避免循环依赖
- 数据流设计: Plan → Doing → Learning 的单向数据流

#### 开发规范

- 代码风格: 遵循 Go 官方代码规范（gofmt, golint）
- 测试要求:
  - 单元测试覆盖率 >= 70%
  - 每个任务必须有可自动化验证的测试脚本
  - 测试脚本使用 Python JSON 格式
- 文档要求:
  - 每个模块必须有对应的 Wiki 文档
  - 文档使用 Markdown 格式
  - 文档必须包含：概述、核心类型、关键函数、类图、使用示例
  - 文档必须使用 Mermaid 图表辅助说明
- 路径规范:
  - **任务描述中必须使用绝对路径或明确的相对路径起点**
  - 项目工作空间: `.rick/`
  - Wiki 文档: `.rick/wiki/`
  - Job 目录: `.rick/jobs/job_n/`
  - 测试脚本: `.rick/jobs/job_n/doing/test_scripts/`
- 任务粒度规范:
  - 每个任务的预计执行时间: 5-10 分钟
  - 任务目标必须清晰、可衡量
  - 任务必须有明确的输入和输出
  - 任务的关键结果必须可自动化验证

#### 工程实践

- 版本控制:
  - 使用 Git 管理代码和文档
  - 每个任务完成后自动 commit
  - Commit message 遵循 Conventional Commits 规范
  - Co-Authored-By 标注 AI 协作
- 持续集成:
  - 自动运行测试脚本
  - 自动验证文档质量
  - 自动检查代码规范
- 发布流程:
  - 使用 `scripts/build.sh` 构建
  - 使用 `scripts/install.sh` 安装
  - 支持生产版本和开发版本并行
- 任务执行流程:
  - Plan: 将需求分解为 DAG 任务图
  - Doing: 按拓扑排序串行执行任务
  - Learning: 总结经验，提取可复用知识
- DAG 设计规范:
  - 使用三层结构：基础层 → 生产层 → 验证层
  - 识别并行化机会，提高执行效率
  - 在阶段性成果完成后设置汇聚点
  - 避免循环依赖
  - 每个任务的依赖关系必须明确

### 修改现有规范

#### 开发规范: 日志系统（修改）

**原内容**:
```
- 日志系统: 使用 Go 标准库 log
```

**更新为**:
```
- 日志系统:
  - 使用 Go 标准库 log，文本格式
  - 最小化日志输出，避免干扰 AI Agent
  - 日志级别: ERROR（错误）、INFO（关键信息）
  - 避免 DEBUG 级别日志
```

**修改理由**: 基于 Morty 的经验，过多的日志会干扰 AI Agent 的理解，应该最小化日志输出

#### 工程实践: 测试策略（修改）

**原内容**:
```
- 测试要求: 单元测试覆盖率 >= 70%
```

**更新为**:
```
- 测试要求:
  - 单元测试覆盖率 >= 70%（Go testing）
  - 集成测试: 使用 Shell 脚本（scripts/test_*.sh）
  - 任务测试: 使用 Python JSON 格式测试脚本
  - 测试脚本必须提供清晰的错误信息（包含完整路径）
  - 测试脚本必须返回明确的成功/失败状态（exit code）
```

**修改理由**: Job 1 中发现测试脚本的错误信息不够清晰，需要明确测试脚本的规范

### 删除规范

无需删除的规范。

## 格式规范

**必须遵守**:
1. 使用 plan.md 中定义的四个主要类别：
   - 技术栈
   - 架构设计
   - 开发规范
   - 工程实践
2. 每个类别下使用 `- 项目名: 内容` 格式
3. 保持层级结构清晰（使用 #### 和 - 符号）
4. 删除的内容使用 `~~删除线~~` 标记

## 合并指令

建议人工审核后执行以下操作：

1. **创建 `.rick/SPEC.md` 文件**（如果不存在）
   ```bash
   touch .rick/SPEC.md
   ```

2. **将上述"新增规范"部分的内容复制到 `.rick/SPEC.md`**
   - 保持四个主要类别的结构
   - 使用 `- 项目名: 内容` 格式

3. **修改现有规范**
   - 找到"日志系统"和"测试策略"相关内容
   - 按照"修改现有规范"部分更新

4. **验证格式**
   - 确认所有规范清晰可执行
   - 检查类别的完整性（技术栈、架构设计、开发规范、工程实践）
   - 确认与 plan.md 中定义的 SPEC 格式完全一致

5. **提交更新**
   ```bash
   git add .rick/SPEC.md
   git commit -m "docs(spec): update SPEC based on job_1 learnings"
   ```

## 关键改进点

### 1. 路径规范明确化

**问题**: Job 1 中 task4 和 task7 因路径歧义导致重试

**改进**: 在 SPEC 中明确规定：
- 任务描述中必须使用绝对路径或明确的相对路径起点
- 列出所有关键目录的标准路径

**预期效果**: 避免未来任务因路径歧义导致的重试

### 2. 任务粒度规范化

**问题**: 如何确定合理的任务粒度？

**改进**: 在 SPEC 中明确规定：
- 每个任务的预计执行时间: 5-10 分钟
- 任务目标必须清晰、可衡量
- 任务必须有明确的输入和输出

**预期效果**: 提高任务设计质量，降低失败风险

### 3. DAG 设计规范化

**问题**: 如何设计合理的 DAG 任务图？

**改进**: 在 SPEC 中明确规定：
- 使用三层结构：基础层 → 生产层 → 验证层
- 识别并行化机会
- 在阶段性成果完成后设置汇聚点
- 避免循环依赖

**预期效果**: 提高 DAG 设计质量，提升执行效率

### 4. 测试脚本规范化

**问题**: 测试脚本的错误信息不够清晰

**改进**: 在 SPEC 中明确规定：
- 测试脚本必须提供清晰的错误信息（包含完整路径）
- 测试脚本必须返回明确的成功/失败状态

**预期效果**: 提高问题定位效率，降低调试成本

## 总结

这些 SPEC 更新基于 Job 1 的实际执行经验，旨在：
1. **避免重复问题**: 路径歧义、任务粒度不当等
2. **提高执行效率**: DAG 设计规范、并行化机会识别
3. **提升文档质量**: 文档标准、验证工具
4. **增强可维护性**: 清晰的规范和最佳实践

建议人工审核后尽快合并到 `.rick/SPEC.md`，为未来的任务执行提供明确的指导。
