# Research 协议重构草稿(v2)

> 2026-08-03 | 草稿待确认 | 触发:v1(BFS+R7)简化讨论

---

## 核心设计

3 个概念替代 v1 的 5 个:

1. **尽调树**:树结构,每层 MECE 划分(不重复不遗漏),节点产生疑问则下钻
2. **信源加权**:置信度 = Σ(信源验证结果 × 权重)
3. **subagent 上下文隔离**:主 research 维护树+主报告,具体调研派给 subagent 落盘

**终止条件**:所有叶节点置信度 = 高(≥ 0.8)。无法达高的叶节点附理由 R7 上报。

**角色**:research = main agent + 调度器。**不直接调研**,只维护树、配置信源、派发 subagent、整合报告。

---

## 工作流

### Step 0:初始化

读任务派发,绘制根节点(=主题),按 MECE 划分第一层子节点。创建主报告 `{{loop_dir}}/briefs/research-report.md`。

### Step 1:信源配置

| 信源 | 默认权重 | 验证方式 |
|---|---|---|
| 代码原文 | 0.4 | Read/Grep |
| 运行时行为 | 0.3 | Bash 跑命令/测试/日志 |
| 文档 | 0.2 | WebFetch/Read |
| 反事实 | 0.1 | 修改代码看影响后还原 |

置信度 = Σ(信源验证结果 × 权重),结果 ∈ {0,1}。高 ≥ 0.8(终止)| 中 0.5-0.8(续研)| 低 < 0.5(R7 上报)。

权重可在 `.rick/config.json` 的 `human_loop.research_source_weights` 覆盖,和应 = 1.0。

### Step 2:调研循环

```
LOOP:
  a. 选节点:置信度 < 高的叶节点(优先 think 选出的最高风险假设对应分支)
     全部叶节点置信度 = 高 → 退出
  b. 派发 subagent(传:节点路径+信源配置+调研动作约束)
     执行:运行程序 / 修改代码(后 git restore 还原) / 获取信息
     返回:各信源验证结果(0/1)+ 证据 + 疑问点
     落盘:briefs/research-{N}-{节点路径}.md
  c. 计算置信度:加权汇总(只在主 research)
  d. 下钻判定:置信度 < 高 且含疑问点 → 按 MECE 划分子节点加入树
     无疑问点但置信度低 → R7 上报,不再下钻
  e. 更新主报告 → 回到 a
```

### Step 3:输出

整合所有 subagent 报告:整合摘要(总节点数/高置信度叶节点数/R7 上报项)+ R7 上报项清单 → 呈现 sense_loop → human 决策。

---

## 尽调树

尽调树 = 根节点(主题)+ 若干层 MECE 划分。每个节点要么是叶节点(已尽调),要么有子节点(已下钻)。

### MECE 划分原则

每层子节点必须:
- **完备**:覆盖父节点全部关注面(不遗漏)
- **互斥**:子节点之间不重叠(不重复)
- **可验证**:每个子节点是可被信源验证的事实陈述

划分维度由模型自行判断(结构/流程/产物等),不限定规则。

### 下钻触发

节点置信度 < 高 **且** subagent 报告含"疑问点" → 按 MECE 把疑问点划为子节点加入树。无疑问点但置信度低 → R7 上报,不再下钻。

### 示例(帮助理解,非穷举规则)

主题:human-loop 命令实现

```
根:human-loop 命令实现
├─ 入口与参数处理(结构维度)
│  ├─ 命令注册 ✅0.9
│  ├─ 参数解析 ✅0.8
│  └─ 配置加载(下钻,疑问:加载顺序?)
│     ├─ 默认配置 ✅0.9
│     ├─ 用户配置覆盖 ✅0.85
│     └─ 环境变量(R7,无法验证)
├─ prompt 生成(流程维度)✅0.9
└─ 产物落盘(产物维度)✅0.8
```

同父节点的子节点可用不同维度划分,由模型自行选择。

### 节点状态

| 状态 | 含义 | 置信度 |
|---|---|---|
| 待调研 | 初始/已派发 | N/A |
| 已澄清 | 事实明确 | ≥ 0.8 |
| 部分澄清 | 置信度中 | 0.5-0.8 |
| 无法澄清 | 置信度低,无疑问点 | < 0.5(R7) |
| 已下钻 | 拆分为子节点 | 看子节点 |

### 树规模约束

深度 ≤ 5 | 每层子节点 ≤ 7 | 总节点 ≤ 30(超过则强制 R7 上报剩余)

---

## 主报告格式

文件:`{{loop_dir}}/briefs/research-report.md`

```markdown
# 调研报告 — [主题] — [日期]

## 信源配置
[信源表 + 加权公式 + 高置信度阈值]

## 尽调树(快照)
[树形结构,标注每个叶节点置信度]

## 节点详情
### [节点路径]:[事实陈述]
- 置信度:[值](高/中/低)
- 信源验证:[各信源 ✅/❌ + 证据]
- 调研报告:briefs/research-{N}-{节点路径}.md
- 子节点(若下钻):[列表]

## R7 上报项(无法达高置信度的叶节点)
- [节点路径]:[理由]

## 整合摘要
总节点数 N | 高置信度叶节点 M | R7 上报 K
```

---

## subagent 报告格式

文件:`{{loop_dir}}/briefs/research-{N}-{节点路径}.md`

```markdown
# 调研 subagent — 节点 [节点路径] — 第 N 轮

## 接收
节点路径 + 事实陈述 + 信源配置 + 调研动作约束

## 执行的调研动作
[动作类型]:[命令/文件] → [结果证据]

## 各信源验证结果
[信源表:✅/❌ + 证据]

## 还原确认
- [ ] 修改的代码已 git restore
- [ ] 写命令已还原(如有)

## 返回(给主 research)
各信源验证结果 [0/1, ...] + 疑问点(若需下钻):[子问题列表]
```

---

## 安全约束

1. 修改代码必须 `git restore` 还原,主 research 整合前检查"还原确认"段
2. 运行程序优先只读命令,写命令需先备份
3. subagent 不持全树上下文,只接收节点路径+信源配置
4. subagent 报告必须落盘(供 learning 复盘)
5. 置信度计算只在主 research(避免 subagent 私调权重)

---

## 兼容性

| 文件 | 改动 |
|---|---|
| research.md | 删 BFS+R7 段,新增 Step 0/1/2/3 + 尽调树引用 |
| skill_research.md | 删 BFS 方法论,新增尽调树+MECE+下钻方法论 |
| config.go | 新增 `HumanLoopConfig.ResearchSourceWeights map[string]float64`,默认 0.4/0.3/0.2/0.1 |
| 测试 | 改 `TestResearchTemplateHasBFS` → `TestResearchTemplateHasDiligenceTree`,断言:尽调树/MECE/下钻/信源/权重/置信度/subagent |

历史 loop_1/prompts/research.md 不动。learning.md 的 briefs/E1.md 引用不变。think.md 不变。

---

## 待用户确认

1. 信源权重默认 0.4/0.3/0.2/0.1?
2. subagent 复用 general-purpose?(推荐)
3. git restore 还原作为硬约束?
4. 置信度 ≥ 0.8 = 高阈值?
5. 树规模上限 深度 5 / 每层 7 / 总数 30?

确认后落地 research.md(~85) + skill_research.md(~65) + config.go(~5) + 测试(~30),共 ~185 行,1 commit。
